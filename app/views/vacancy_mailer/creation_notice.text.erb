<%= t(".body.text", :title => @vacancy.title,
                    :description => @vacancy.description,
                    :company => @vacancy.company,
                    :email => @vacancy.email,
                    :url => @vacancy.url,
                    :vacancy_url => vacancy_url(@vacancy, :token => @vacancy.admin_token)) %>